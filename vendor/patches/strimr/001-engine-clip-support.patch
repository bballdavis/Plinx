diff --git a/Shared/Features/MediaDetail/MediaDetailViewModel.swift b/Shared/Features/MediaDetail/MediaDetailViewModel.swift
index 75c4c90..984c720 100644
--- a/Shared/Features/MediaDetail/MediaDetailViewModel.swift
+++ b/Shared/Features/MediaDetail/MediaDetailViewModel.swift
@@ -230,7 +230,7 @@ final class MediaDetailViewModel {
 
     var primaryActionTitle: String {
         switch media.type {
-        case .movie:
+        case .movie, .clip:
             hasProgress(for: media.mediaItem)
                 ? String(localized: "common.actions.resume")
                 : String(localized: "common.actions.play")
@@ -247,7 +247,7 @@ final class MediaDetailViewModel {
 
     var primaryActionDetail: String? {
         switch media.type {
-        case .movie:
+        case .movie, .clip:
             return timeLeftText(for: media.mediaItem)
         case .show:
             guard let onDeckItem else { return nil }
@@ -264,7 +264,7 @@ final class MediaDetailViewModel {
 
     var primaryActionProgress: Double? {
         switch media.type {
-        case .movie:
+        case .movie, .clip:
             return progressFraction(for: media.mediaItem)
         case .show:
             guard let onDeckItem else { return nil }
@@ -276,7 +276,7 @@ final class MediaDetailViewModel {
 
     var shouldShowPlayFromStartButton: Bool {
         switch media.type {
-        case .movie:
+        case .movie, .clip:
             hasProgress(for: media.mediaItem)
         case .show:
             hasProgress(for: onDeckItem)
@@ -287,7 +287,7 @@ final class MediaDetailViewModel {
 
     var primaryActionRatingKey: String? {
         switch media.type {
-        case .movie:
+        case .movie, .clip:
             media.id
         case .show:
             onDeckItem?.id
@@ -335,7 +335,7 @@ final class MediaDetailViewModel {
         guard let playableType = PlayableItemType(plexType: item.type) else { return false }
 
         switch playableType {
-        case .movie, .episode:
+        case .movie, .episode, .clip:
             return (item.viewCount ?? 0) > 0
         case .show, .season:
             guard let leafCount = item.leafCount, let viewedLeafCount = item.viewedLeafCount else {
diff --git a/Shared/Features/Search/SearchResultCard.swift b/Shared/Features/Search/SearchResultCard.swift
index 03cdf1c..c22d5fe 100644
--- a/Shared/Features/Search/SearchResultCard.swift
+++ b/Shared/Features/Search/SearchResultCard.swift
@@ -91,6 +91,8 @@ private struct TypeBadge: View {
             String(localized: "search.badge.collection")
         case .playlist:
             String(localized: "search.badge.playlist")
+        case .clip:
+            String(localized: "search.badge.clip")
         case .unknown:
             String(localized: "search.badge.unknown")
         }
@@ -110,6 +112,8 @@ private struct TypeBadge: View {
             .teal
         case .playlist:
             .indigo
+        case .clip:
+            .orange
         case .unknown:
             .gray
         }
@@ -134,6 +138,8 @@ private extension SearchResultCard {
             media.secondaryLabel ?? String(localized: "search.fallback.collection")
         case .playlist:
             media.secondaryLabel ?? String(localized: "search.fallback.playlist")
+        case .clip:
+            media.secondaryLabel ?? media.title
         case .unknown:
             media.title
         }
diff --git a/Shared/Models/Library.swift b/Shared/Models/Library.swift
index 2f26588..a70a587 100644
--- a/Shared/Models/Library.swift
+++ b/Shared/Models/Library.swift
@@ -14,6 +14,8 @@ struct Library: Identifiable, Equatable, Hashable {
             "tv.fill"
         case .season, .episode:
             "play.rectangle.fill"
+        case .clip:
+            "video.fill"
         case .collection, .playlist, .unknown:
             "questionmark.square.fill"
         }
diff --git a/Shared/Models/MediaDisplayItem.swift b/Shared/Models/MediaDisplayItem.swift
index 6c1cc18..7b30634 100644
--- a/Shared/Models/MediaDisplayItem.swift
+++ b/Shared/Models/MediaDisplayItem.swift
@@ -187,7 +187,7 @@ enum MediaDisplayItem: Identifiable, Hashable {
 extension MediaDisplayItem {
     init?(plexItem: PlexItem) {
         switch plexItem.type {
-        case .movie, .show, .season, .episode:
+        case .movie, .show, .season, .episode, .clip:
             self = .playable(MediaItem(plexItem: plexItem))
         case .collection:
             self = .collection(CollectionMediaItem(plexItem: plexItem))
diff --git a/Shared/Models/MediaItem+WatchStatus.swift b/Shared/Models/MediaItem+WatchStatus.swift
index f93af0b..7fe31a0 100644
--- a/Shared/Models/MediaItem+WatchStatus.swift
+++ b/Shared/Models/MediaItem+WatchStatus.swift
@@ -3,7 +3,7 @@ import Foundation
 extension MediaItem {
     var isFullyWatched: Bool {
         switch type {
-        case .movie, .episode:
+        case .movie, .episode, .clip:
             return (viewCount ?? 0) > 0
         case .show, .season:
             guard let leafCount, let viewedLeafCount, leafCount > 0 else {
diff --git a/Shared/Models/MediaItem.swift b/Shared/Models/MediaItem.swift
index 17afe4a..9ee2e8e 100644
--- a/Shared/Models/MediaItem.swift
+++ b/Shared/Models/MediaItem.swift
@@ -50,7 +50,7 @@ struct MediaItem: Identifiable, Hashable {
 
     var secondaryLabel: String? {
         switch type {
-        case .movie:
+        case .movie, .clip:
             return year.map(String.init)
 
         case .show:
@@ -83,7 +83,7 @@ struct MediaItem: Identifiable, Hashable {
             grandparentRatingKey ?? parentRatingKey ?? id
         case .season:
             parentRatingKey ?? id
-        case .movie, .show:
+        case .movie, .show, .clip:
             id
         case .collection, .playlist, .unknown:
             id
diff --git a/Shared/Models/PlayableMediaItem.swift b/Shared/Models/PlayableMediaItem.swift
index 401f201..7048777 100644
--- a/Shared/Models/PlayableMediaItem.swift
+++ b/Shared/Models/PlayableMediaItem.swift
@@ -5,6 +5,8 @@ enum PlayableItemType: String, Codable, Hashable {
     case show
     case season
     case episode
+    /// Plex "Other Videos" / clip library items
+    case clip
 
     init?(plexType: PlexItemType) {
         switch plexType {
@@ -16,6 +18,8 @@ enum PlayableItemType: String, Codable, Hashable {
             self = .season
         case .episode:
             self = .episode
+        case .clip:
+            self = .clip
         case .collection, .playlist, .unknown:
             return nil
         }
@@ -31,6 +35,8 @@ enum PlayableItemType: String, Codable, Hashable {
             .season
         case .episode:
             .episode
+        case .clip:
+            .clip
         }
     }
 }
diff --git a/Shared/Models/Plex/PlexMediaModels.swift b/Shared/Models/Plex/PlexMediaModels.swift
index a3738d2..c351c1b 100644
--- a/Shared/Models/Plex/PlexMediaModels.swift
+++ b/Shared/Models/Plex/PlexMediaModels.swift
@@ -7,6 +7,8 @@ enum PlexItemType: String, Codable {
     case episode
     case collection
     case playlist
+    /// Plex "Other Videos" libraries — type = "clip" on the server
+    case clip
     case unknown
 
     var isSupported: Bool {
@@ -15,7 +17,7 @@ enum PlexItemType: String, Codable {
 
     var isPlayable: Bool {
         switch self {
-        case .movie, .show, .season, .episode:
+        case .movie, .show, .season, .episode, .clip:
             true
         case .collection, .playlist, .unknown:
             false
diff --git a/Strimr-iOS/Features/Downloads/DownloadModels.swift b/Strimr-iOS/Features/Downloads/DownloadModels.swift
index 7ac71a1..df3e41b 100644
--- a/Strimr-iOS/Features/Downloads/DownloadModels.swift
+++ b/Strimr-iOS/Features/Downloads/DownloadModels.swift
@@ -52,7 +52,7 @@ struct DownloadedMediaMetadata: Codable, Hashable {
             return parentTitle
         case .show:
             return nil
-        case .collection, .playlist, .unknown:
+        case .collection, .playlist, .clip, .unknown:
             return nil
         }
     }
diff --git a/Strimr-iOS/Features/MediaDetail/MediaDetailHeaderSection.swift b/Strimr-iOS/Features/MediaDetail/MediaDetailHeaderSection.swift
index 74be155..9a8c8d7 100644
--- a/Strimr-iOS/Features/MediaDetail/MediaDetailHeaderSection.swift
+++ b/Strimr-iOS/Features/MediaDetail/MediaDetailHeaderSection.swift
@@ -415,7 +415,7 @@ struct MediaDetailHeaderSection: View {
             Task {
                 await downloadManager.enqueueSeason(ratingKey: viewModel.media.id, context: context)
             }
-        case .movie, .episode:
+        case .movie, .episode, .clip:
             Task {
                 await downloadManager.enqueueItem(ratingKey: viewModel.media.id, context: context)
             }
diff --git a/Strimr-iOS/Features/WatchTogether/WatchTogetherView.swift b/Strimr-iOS/Features/WatchTogether/WatchTogetherView.swift
index 9518926..daf072d 100644
--- a/Strimr-iOS/Features/WatchTogether/WatchTogetherView.swift
+++ b/Strimr-iOS/Features/WatchTogether/WatchTogetherView.swift
@@ -323,6 +323,8 @@ private struct WatchTogetherSelectedMediaCard: View {
             String(localized: "search.badge.collection")
         case .playlist:
             String(localized: "search.badge.playlist")
+        case .clip:
+            String(localized: "search.badge.clip")
         case .unknown:
             String(localized: "search.badge.unknown")
         }
