# PlinxApp

Generate the Xcode project using XcodeGen from the `project.yml` file in this folder.

This keeps the project structure deterministic and upstream-friendly.

For the canonical setup, testing, and architecture guidance, use `docs/` from the repo root.
